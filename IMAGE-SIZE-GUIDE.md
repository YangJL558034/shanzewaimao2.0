# 全站图片设计与上传尺寸规范

本规范对应当前前台组件和后台上传入口。尺寸均为推荐导出尺寸，不要求上传图片与浏览器显示像素完全相同；保持表格中的宽高比，可避免裁切变形。

## 通用导出规则

- 摄影图优先使用 WebP，其次 JPG；透明图标、Logo、认证标识使用 SVG 或透明 PNG。
- 普通图片建议控制在 500 KB 内，首屏背景建议控制在 700 KB 内，Logo 和图标建议控制在 150 KB 内。
- 图片使用 sRGB 色彩空间，不要在图片中写标题、按钮或联系方式，文字由后台单独管理。
- `cover` 类型会裁切图片边缘，主体四周预留约 10% 安全区；`contain` 类型会完整显示图片。
- 产品详情轮播建议全部使用相同比例，避免切换时画面跳动。

## 图片尺寸总表

| 使用位置 | 后台入口 | 推荐尺寸 | 比例 | 显示方式与设计要求 |
| --- | --- | ---: | ---: | --- |
| 网站图标 Logo（顶部、底部） | 站点设置 → Logo | 512 × 512 px | 1:1 | SVG/透明 PNG；这里只上传图形标志，公司名称由后台文字显示 |
| 首页首屏背景/轮播 | 首页管理 → 首屏轮播，或 Banner 管理 → `home` | 1920 × 720 px | 8:3 | `cover`；主体放在画面右侧 55%–82%，左侧留给标题 |
| 关于我们首屏 | 首页管理顶部页面状态栏 → 关于我们 → 首屏轮播 | 1920 × 720 px | 8:3 | 与首页首屏相同 |
| 产品中心首屏 | 页面状态栏 → 产品中心 → 首屏轮播 | 1920 × 720 px | 8:3 | 产品主体放右侧，背景尽量简洁 |
| 工厂实力首屏 | 页面状态栏 → 工厂实力 → 首屏轮播 | 1920 × 720 px | 8:3 | 生产线主体放中右侧 |
| 品质保障首屏 | 页面状态栏 → 品质保障 → 首屏轮播 | 1920 × 720 px | 8:3 | 实验室或产品主体放中右侧 |
| 新闻资讯首屏 | 页面状态栏 → 新闻资讯 → 首屏轮播 | 1920 × 720 px | 8:3 | 建筑或新闻主题放右侧 |
| 联系我们首屏 | 页面状态栏 → 联系我们 → 首屏轮播 | 1920 × 720 px | 8:3 | 产品或团队主体放右侧 |
| 首页公司简介配图 | 首页管理 → 公司简介区块 | 1200 × 800 px | 3:2 | `cover`；建筑主体居中 |
| 关于我们“我们的故事”配图 | 关于我们 → 页面区块 | 1200 × 800 px | 3:2 | `cover`；建筑或团队照片 |
| 关于我们普通卡片配图 | 关于我们 → 页面区块 → 卡片项目 | 800 × 600 px | 4:3 | `cover`；每组图片比例保持一致 |
| 关于我们全球业务/地图配图 | 关于我们 → 页面区块 | 1600 × 900 px | 16:9 | 地图重要标记不要靠边 |
| 产品分类图片 | 产品分类 | 800 × 600 px | 4:3 | `cover`；产品居中并保留白边 |
| 产品列表封面 | 产品管理 → 产品封面图片 | 1400 × 1400 px | 1:1 | 列表使用 `cover`，详情主图使用 `contain`；白色或浅色背景 |
| 产品详情轮播图 | 产品管理 → 产品详情轮播图 | 1400 × 1400 px | 1:1 | 建议每个产品上传 4–8 张；主图、背面、侧面、颜色和应用场景均用同一比例 |
| 产品应用场景卡片 | 产品中心 → 页面区块 → 应用场景 | 800 × 600 px | 4:3 | `cover`；人物和产品不要贴边 |
| 产品目录封面图 | 产品中心 → 页面区块 → 下载目录 | 1200 × 1600 px | 3:4 | 适合目录书封面；PDF 文件另行上传 |
| 工厂概览配图 | 工厂实力 → 页面区块 → 工厂概览 | 1200 × 800 px | 3:2 | `cover`；建筑或车间全景 |
| 工厂设备图片 | 工厂设备 | 1200 × 900 px | 4:3 | `cover`；同组设备保持相同机位和比例 |
| 车间环境图片 | 车间环境 | 1200 × 900 px | 4:3 | `cover`；用于首页、工厂和品质页面轮播 |
| 实验室与可靠性测试 | 品质保障 → 页面区块 → 实验室测试项目 | 1200 × 900 px | 4:3 | `cover`；一项测试对应一张图片 |
| 质量流程项目图片 | 品质保障 → 页面区块 → 质量控制项目 | 800 × 600 px | 4:3 | `cover`；也可不上传，系统显示线性图标 |
| 认证资质标识 | 证书管理 → 证书标识图片 | 800 × 500 px | 8:5 | `contain`；透明 PNG/SVG，标识四周留白 12% |
| 证书扫描件 | 证书管理 → 证书文件 | 1600 × 2200 px | 约 8:11 | 建议 PDF；若为图片请保证文字可读 |
| 新闻封面图 | 新闻管理 → 新闻封面图片 | 1600 × 900 px | 16:9 | `cover`；列表、详情和社交分享均可复用 |
| 联系页面地图/地址背景 | 联系我们 → 页面区块 → 联系信息背景图 | 1600 × 640 px | 5:2 | `cover`；地图定位点放在中间安全区 |
| 页面区块通用大配图 | 各页面管理 → 页面区块 → 区块背景/配图 | 1200 × 800 px | 3:2 | 用于左右图文布局 |
| 页面区块通用卡片图 | 各页面管理 → 页面区块 → 卡片项目图片 | 800 × 600 px | 4:3 | 用于卖点、应用、质量等卡片 |
| 流程步骤小图标 | 工厂实力 → 页面区块 → 流程项目 | 256 × 256 px | 1:1 | SVG/透明 PNG；未上传时显示系统图标 |
| 联系信息小图标 | 联系我们 → 页面区块 → 联系项目 | 256 × 256 px | 1:1 | SVG/透明 PNG；未上传时显示系统图标 |
| SEO/Open Graph 分享图 | SEO 管理 → Open Graph 图片 | 1200 × 630 px | 1.91:1 | 社交平台标准分享比例，关键信息放中间 80% |
| 后台管理员头像 | 用户与权限 → 管理员 | 400 × 400 px | 1:1 | 正方形头像；当前无头像时显示姓名首字母 |
| 后台登录背景图 | 项目登录视觉资源 | 1920 × 1080 px | 16:9 | 当前使用工厂视觉背景，页面会添加蓝色遮罩 |

## 首屏背景的移动端安全区

前台桌面端和移动端共用同一张首屏图片，因此设计时需要同时兼顾：

1. 图片中不要直接放文字、Logo 或按钮。
2. 主产品或工厂主体放在横向 55%–78%、纵向 25%–75% 范围。
3. 左侧约 45% 保持干净，用于桌面端标题。
4. 四边至少保留 80 px 安全区，移动端裁切后主体仍应完整。
5. 同一页面的多张轮播图必须使用相同的 1920 × 720 尺寸。

## 产品图片拍摄顺序建议

每个产品建议上传 4–8 张 1400 × 1400 图片，并按以下顺序排序：

1. 白底正面主图。
2. 背面或接口细节。
3. 侧面厚度。
4. 不同颜色。
5. 手机或设备安装效果。
6. 商务/旅行应用场景。
7. 包装盒及配件。
8. OEM Logo 或包装定制效果。

